<h1>Todays tasks are:</h1>

<ul>
  <% @tasks.each do |task| %>
  <li><%= check_box_tag(@task) %> <%= link_to task.title, task_path(task) %></li>
  <% end %>
</ul>

<div class="border-top p-3">
  <%= link_to '+ New task', new_task_path %>
</div>
